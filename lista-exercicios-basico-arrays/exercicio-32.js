// 32 - Adicione um novo elemento ao início do array.
const array = [1, 2, 3, 4, 5];

console.log('Array atual:', array);

array.unshift(0);

console.log('Array com um elemento adicionado no início', array);
