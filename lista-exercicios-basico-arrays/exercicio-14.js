// 14 - Multiplique cada elemento do array por 2.

const array = [3, 2, 5, 7, 9];

// forEach()
// array.forEach((element) => console.log(element * 2));

// for
for(let i = 0; i < array.length; i++) {
  let numero = array[i] * 2;
  console.log(numero);
}