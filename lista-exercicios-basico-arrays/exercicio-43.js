// 43 - Inverta a ordem dos elementos do array.
const array = [0, 1, 2, 3, 4, 5];
console.log('Array normal:', array);

const arrayInvertido = array.reverse();

console.log('Array invertido:', arrayInvertido);
