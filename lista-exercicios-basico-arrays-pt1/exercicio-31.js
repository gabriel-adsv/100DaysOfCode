// 31 - Remova o último elemento do array.
const array = [1, 2, 3, 4, 5];

console.log('Array antes da remoção do último elemento:', array);

array.pop();

console.log('Array com o último elemento removido:', array);
